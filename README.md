# English Tutor Voice Agent (MVP)

Голосовой ИИ-репетитор английского языка на базе Google Gemini Realtime Model.

## О Проекте

Это **MVP** (Minimum Viable Product) для голосового репетитора английского языка.
Агент помогает практиковать разговорный английский, обсуждая различные темы и исправляя ошибки в реальном времени.

## Возможности MVP

- 🎤 **Голосовое взаимодействие** через LiveKit Cloud
- 🤖 **Google Gemini** (gemini-2.5-flash-native-audio-preview-12-2025)
- 🗣️ **Интерактивное общение** - агент поддерживает диалог на любую тему
- ✏️ **Исправление грамматики** в речи пользователя
- 📹 **Видео поддержка** - может видеть и анализировать видео (экран или камера)

## Архитектура

```
[Пользователь] ↔ [LiveKit Cloud] ↔ [Ваш сервер (Python Agent)]
                                     └─ [Google Gemini Realtime Model]
```

## Настройка окружения

Создайте файл `.env` или укажите следующие переменные окружения на вашем хостинге:

### LiveKit Configuration
- `LIVEKIT_URL` = `wss://your-livekit-server.livekit.cloud`
- `LIVEKIT_API_KEY` = ваш API ключ
- `LIVEKIT_API_SECRET` = ваш секретный ключ

### AI Model
- `GOOGLE_API_KEY` = ваш Google Gemini API ключ

## Как Получить Ключи

### 1. LiveKit Cloud
1. Зайдите на [livekit.io](https://livekit.io) и зарегистрируйтесь
2. Создайте новый проект
3. Перейдите в Settings → Keys & Tokens
4. Скопируйте:
   - WebSocket URL (начинается с `wss://`)
   - API Key
   - Secret Key

### 2. Google Gemini API
1. Зайдите на [Google AI Studio](https://aistudio.google.com/apikey)
2. Создайте новый API ключ
3. Скопируйте ключ (начинается с `AIza...`)

## Запуск в Docker (Рекомендуется для сервера)

### 1. Подготовка
Убедитесь, что у вас установлен `docker` и `docker-compose`. Скопируйте `.env.example` в `.env` и заполните ключи.

### 2. Запуск через Docker Compose
Это самый простой способ:
```bash
docker compose up -d --build
```
Эта команда соберет образ и запустит контейнер в фоновом режиме.

### 3. Ручная сборка и запуск
Если вы не хотите использовать compose:
```bash
# Сборка образа
docker build -t english-tutor-agent .

# Запуск контейнера
docker run -d --name english-tutor-agent --env-file .env english-tutor-agent
```

## Запуск локально (без Docker)

## Тестирование

### Через LiveKit Playground

1. Убедитесь, что агент запущен и подключен к LiveKit.
2. Откройте [LiveKit Playground](https://agents-playground.livekit.io)
3. Введите данные подключения:
   - **URL**: ваш `LIVEKIT_URL`
   - **API Key**: ваш `LIVEKIT_API_KEY`
   - **Secret**: ваш `LIVEKIT_API_SECRET`
4. Нажмите **Connect**
5. Разрешите доступ к микрофону
6. Начните общение с агентом.

## Технологии

- **Backend**: Python 3.11+
- **Framework**: LiveKit Agents
- **AI Model**: Google Gemini Realtime (gemini-2.5-flash-native-audio-preview-12-2025)
- **Voice**: Aoede (женский голос)
- **Audio**: Silero VAD

## Структура Проекта

```
aijarvis/
├── agent.py           # Основной код агента
├── requirements.txt   # Python зависимости
├── .env.example      # Шаблон переменных окружения
└── .gitignore       # Git ignore
```

## Troubleshooting

### Agent не стартует
- Проверьте переменные окружения.
- Убедитесь, что `LIVEKIT_URL` начинается с `wss://`.
- Проверьте доступность Google API.

### Нет звука
- Проверьте, что микрофон разрешен в браузере.
- Попробуйте переподключиться в LiveKit Playground.

## License

MIT

---

🤖 Powered by Google Gemini Realtime API & LiveKit Cloud

